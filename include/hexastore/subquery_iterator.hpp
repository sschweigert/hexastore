#ifndef _HEXASTORE_SUBQUERY_ITERATOR_HPP_
#define _HEXASTORE_SUBQUERY_ITERATOR_HPP_

/*
template <class Functor, class ...Args>
SubQueryIterator::SubQueryIterator<Functor, Args...>(Hexastore& hexastore, const MiddleNode& root, Functor& functor, Args... args) :
	hexastore(hexastore),
	functor(functor),
	middleIterator(middleNode->begin()),
	bottomIterator(middleIterator->second.begin()),
	middleNode(&root),
	subQueryIterator(hexastore, hexastore.roots[spo].data[*bottomIterator], args...)
{
}

*/
#endif
